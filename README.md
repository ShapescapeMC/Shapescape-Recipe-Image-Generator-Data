The repository should contain two additional floders:
- `block-images` - this should contain rendred block images they should look the same as in the inventory.
- `RP` - this should contain a vanilla resource pack.
